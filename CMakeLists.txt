cmake_minimum_required(VERSION 3.16)
set(_fluidgrain_top_level OFF)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(fluidgrain VERSION 0.1.0 LANGUAGES C)
  set(_fluidgrain_top_level ON)
endif()

set(FLUIDGRAIN_CUSTOM_CMAKE "${CMAKE_CURRENT_LIST_DIR}/Custom.cmake" CACHE FILEPATH "Optional local settings")
if(EXISTS "${FLUIDGRAIN_CUSTOM_CMAKE}")
  include("${FLUIDGRAIN_CUSTOM_CMAKE}")
endif()
option(FLUIDGRAIN_BUILD_TESTS "Build native regression tests" ${_fluidgrain_top_level})
option(FLUIDGRAIN_INSTALL "Install the runtime module" ${_fluidgrain_top_level})
option(FLUIDGRAIN_SANITIZE "Instrument core/tests/plugin with ASan and UBSan" OFF)
option(FLUIDGRAIN_CUDA "Build the optional offline native CUDA grain opcode" OFF)
option(FLUIDGRAIN_METAL "Use Apple Metal for native worker grain rendering" OFF)
option(FLUIDGRAIN_DESKTOP "Build the shared WebGL desktop instrument" OFF)
if(FLUIDGRAIN_METAL AND (NOT APPLE OR FLUIDGRAIN_CUDA))
  message(FATAL_ERROR "Metal requires macOS and cannot be combined with CUDA")
endif()
set(_fluidgrain_worker_default OFF)
if(UNIX AND NOT CMAKE_SYSTEM_NAME MATCHES "^(Emscripten|WASI)$")
  set(_fluidgrain_worker_default ON)
endif()
option(FLUIDGRAIN_NATIVE_WORKER "Build host-prepared native buffered playback" ${_fluidgrain_worker_default})
if((FLUIDGRAIN_CUDA OR FLUIDGRAIN_METAL) AND NOT FLUIDGRAIN_NATIVE_WORKER)
  message(FATAL_ERROR "Native GPU rendering requires FLUIDGRAIN_NATIVE_WORKER")
endif()

add_library(fluidgrain_core STATIC src/fluidgrain_core.c src/fluidgrain_field.c src/fluidgrain_particles.c src/fluidgrain_resampler.c src/fluidgrain_transport.c src/fluidgrain_provider.c src/fluidgrain_browser.c src/fluidgrain_pack.c)
target_include_directories(fluidgrain_core PUBLIC "${CMAKE_CURRENT_LIST_DIR}/src" "${CMAKE_CURRENT_LIST_DIR}/include")
target_compile_features(fluidgrain_core PUBLIC c_std_11)
set_target_properties(fluidgrain_core PROPERTIES C_STANDARD 11 C_STANDARD_REQUIRED YES C_EXTENSIONS NO POSITION_INDEPENDENT_CODE YES C_VISIBILITY_PRESET hidden)
if(UNIX)
  target_link_libraries(fluidgrain_core PUBLIC m)
endif()
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
  target_compile_options(fluidgrain_core PRIVATE -Wall -Wextra -Wpedantic -Wconversion)
  if(FLUIDGRAIN_SANITIZE)
    target_compile_options(fluidgrain_core PUBLIC -fsanitize=address,undefined,float-cast-overflow -fno-omit-frame-pointer)
    target_link_options(fluidgrain_core PUBLIC -fsanitize=address,undefined,float-cast-overflow)
  endif()
endif()

set(_fluidgrain_source "${CMAKE_CURRENT_LIST_DIR}/src/fluidgrain_opcode.c")
if(COMMAND make_plugin AND DEFINED libcsound_public_include_dirs)
  make_plugin(fluidgrain "${_fluidgrain_source}")
else()
  set(HLOLLI_CSOUND_INCLUDE_DIR "" CACHE PATH "Generated Csound headers")
  set(HLOLLI_CSOUND_BUILD_DIR "${CMAKE_CURRENT_LIST_DIR}/../csound/build" CACHE PATH "Csound build tree")
  set(_fluidgrain_header_target "")
  if(NOT HLOLLI_CSOUND_INCLUDE_DIR AND EXISTS "${HLOLLI_CSOUND_BUILD_DIR}/include/csdl.h")
    set(HLOLLI_CSOUND_INCLUDE_DIR "${HLOLLI_CSOUND_BUILD_DIR}/include")
  endif()
  if(NOT HLOLLI_CSOUND_INCLUDE_DIR AND APPLE AND
     EXISTS "${HLOLLI_CSOUND_BUILD_DIR}/CsoundLib64.framework/Headers/csdl.h")
    set(HLOLLI_CSOUND_INCLUDE_DIR "${HLOLLI_CSOUND_BUILD_DIR}/CsoundLib64.framework/Headers")
  endif()
  if(NOT HLOLLI_CSOUND_INCLUDE_DIR)
    find_package(Csound 7 CONFIG QUIET)
    foreach(candidate IN ITEMS Csound::CsoundLib64 Csound::CsoundLib Csound::Csound CsoundLib64 CsoundLib)
      if(TARGET "${candidate}")
        get_target_property(_includes "${candidate}" INTERFACE_INCLUDE_DIRECTORIES)
        if(_includes AND NOT _includes MATCHES "-NOTFOUND$")
          set(_fluidgrain_header_target "${candidate}")
          break()
        endif()
      endif()
    endforeach()
  endif()
  if(NOT HLOLLI_CSOUND_INCLUDE_DIR AND NOT _fluidgrain_header_target)
    find_path(_fluidgrain_system_include csdl.h HINTS "$ENV{CSOUND_INCLUDE_DIR}" PATH_SUFFIXES csound Headers)
    if(_fluidgrain_system_include)
      set(HLOLLI_CSOUND_INCLUDE_DIR "${_fluidgrain_system_include}")
    endif()
  endif()
  if(HLOLLI_CSOUND_INCLUDE_DIR)
    foreach(header IN ITEMS csdl.h version.h float-version.h)
      if(NOT EXISTS "${HLOLLI_CSOUND_INCLUDE_DIR}/${header}")
        message(FATAL_ERROR "Missing ${header}: use generated <csound-build>/include headers")
      endif()
    endforeach()
  elseif(NOT _fluidgrain_header_target)
    message(FATAL_ERROR "Set HLOLLI_CSOUND_BUILD_DIR or HLOLLI_CSOUND_INCLUDE_DIR to a Csound 7 build")
  endif()
  if(APPLE)
    add_library(fluidgrain SHARED "${_fluidgrain_source}")
  else()
    add_library(fluidgrain MODULE "${_fluidgrain_source}")
  endif()
  if(HLOLLI_CSOUND_INCLUDE_DIR)
    target_include_directories(fluidgrain SYSTEM PRIVATE "${HLOLLI_CSOUND_INCLUDE_DIR}")
  else()
    target_include_directories(fluidgrain SYSTEM PRIVATE "$<TARGET_PROPERTY:${_fluidgrain_header_target},INTERFACE_INCLUDE_DIRECTORIES>")
  endif()
  include(GNUInstallDirs)
  set(_fluidgrain_plugin_folder "plugins64-7.0")
  if(HLOLLI_CSOUND_INCLUDE_DIR)
    file(STRINGS "${HLOLLI_CSOUND_INCLUDE_DIR}/version.h" _major REGEX "^#define[ \t]+CS_VERSION")
    file(STRINGS "${HLOLLI_CSOUND_INCLUDE_DIR}/version.h" _minor REGEX "^#define[ \t]+CS_SUBVER")
    string(REGEX MATCH "[0-9]+" _major "${_major}")
    string(REGEX MATCH "[0-9]+" _minor "${_minor}")
    if(NOT _major STREQUAL "" AND NOT _minor STREQUAL "")
      set(_fluidgrain_plugin_folder "plugins64-${_major}.${_minor}")
    endif()
  endif()
  set(_default_install "${CMAKE_INSTALL_LIBDIR}/csound/${_fluidgrain_plugin_folder}")
  if(DEFINED PLUGIN_INSTALL_DIR AND NOT PLUGIN_INSTALL_DIR STREQUAL "")
    set(_default_install "${PLUGIN_INSTALL_DIR}")
  endif()
  set(HLOLLI_CSOUND_PLUGIN_DIR "${_default_install}" CACHE STRING "Runtime plugin install directory")
  if(FLUIDGRAIN_INSTALL)
    install(TARGETS fluidgrain LIBRARY DESTINATION "${HLOLLI_CSOUND_PLUGIN_DIR}" RUNTIME DESTINATION "${HLOLLI_CSOUND_PLUGIN_DIR}")
    install(FILES LICENSE COPYRIGHT DESTINATION "${CMAKE_INSTALL_DATADIR}/doc/naviergrain")
  endif()
endif()
target_link_libraries(fluidgrain PRIVATE fluidgrain_core)
target_compile_definitions(fluidgrain PRIVATE BUILD_PLUGINS)
set_target_properties(fluidgrain PROPERTIES OUTPUT_NAME naviergrain C_STANDARD 11 C_STANDARD_REQUIRED YES C_EXTENSIONS NO C_VISIBILITY_PRESET hidden POSITION_INDEPENDENT_CODE YES)
if(TARGET sync_build_include_headers)
  add_dependencies(fluidgrain sync_build_include_headers)
endif()
add_library(hlolli::fluidgrain ALIAS fluidgrain)

if(FLUIDGRAIN_CUDA)
  enable_language(CUDA)
  add_library(fluidgrain_cuda STATIC src/fluidgrain_cuda.cu)
  target_include_directories(fluidgrain_cuda PUBLIC "${CMAKE_CURRENT_LIST_DIR}/src")
  set_target_properties(fluidgrain_cuda PROPERTIES CUDA_STANDARD 17
    CUDA_STANDARD_REQUIRED YES POSITION_INDEPENDENT_CODE YES
    CUDA_VISIBILITY_PRESET hidden)
  # Preserve the packet decoder's arithmetic; no fast-math approximation.
  target_compile_options(fluidgrain_cuda PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:--fmad=false>)
  target_compile_definitions(fluidgrain PRIVATE FG_HAVE_CUDA)
endif()

if(FLUIDGRAIN_NATIVE_WORKER)
  find_package(Threads REQUIRED)
  if(NOT CMAKE_USE_PTHREADS_INIT)
    message(FATAL_ERROR "Native buffered playback requires POSIX threads")
  endif()
  add_library(fluidgrain_native_gpu STATIC src/fluidgrain_native_gpu.c src/fluidgrain_gpu_worker.c)
  target_link_libraries(fluidgrain_native_gpu PUBLIC Threads::Threads fluidgrain_core)
  if(FLUIDGRAIN_METAL)
    enable_language(OBJCXX)
    file(READ "${CMAKE_CURRENT_LIST_DIR}/src/fluidgrain_grains.metal" FG_METAL_SOURCE)
    set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS src/fluidgrain_grains.metal)
    configure_file(cmake/metal_source.h.in generated/fluidgrain_metal_source.h @ONLY)
    add_library(fluidgrain_metal STATIC src/fluidgrain_metal.mm)
    target_include_directories(fluidgrain_metal PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/generated")
    target_compile_options(fluidgrain_metal PRIVATE -fobjc-arc -fvisibility=hidden)
    target_link_libraries(fluidgrain_metal PUBLIC "-framework Metal" "-framework Foundation")
    set_target_properties(fluidgrain_metal PROPERTIES OBJCXX_STANDARD 14 POSITION_INDEPENDENT_CODE YES CXX_VISIBILITY_PRESET hidden)
    target_link_libraries(fluidgrain_native_gpu PUBLIC fluidgrain_metal)
  elseif(FLUIDGRAIN_CUDA)
    target_sources(fluidgrain_native_gpu PRIVATE src/fluidgrain_device_cuda.c)
    target_link_libraries(fluidgrain_native_gpu PUBLIC fluidgrain_cuda)
  else()
    target_sources(fluidgrain_native_gpu PRIVATE src/fluidgrain_device_cuda.c src/fluidgrain_cuda_stub.c)
  endif()
  set_target_properties(fluidgrain_native_gpu PROPERTIES C_STANDARD 11
    C_STANDARD_REQUIRED YES C_EXTENSIONS NO POSITION_INDEPENDENT_CODE YES
    C_VISIBILITY_PRESET hidden)
  target_link_libraries(fluidgrain PRIVATE fluidgrain_native_gpu)
  target_compile_definitions(fluidgrain PRIVATE FG_HAVE_NATIVE_WORKER)
endif()

# Runnable native host, also available when regression tests are disabled.
if(FLUIDGRAIN_NATIVE_WORKER AND UNIX)
  find_library(FLUIDGRAIN_CSOUND_LIBRARY NAMES csound64 csound CsoundLib64
    HINTS "${HLOLLI_CSOUND_BUILD_DIR}" "${CMAKE_CURRENT_LIST_DIR}/../csound/build"
    DOC "Matching Csound 7 API library")
  if(FLUIDGRAIN_CSOUND_LIBRARY)
  add_executable(fluidgrain_device_host examples/buffered-device.c)
  target_include_directories(fluidgrain_device_host SYSTEM PRIVATE
    "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
  target_link_libraries(fluidgrain_device_host PRIVATE fluidgrain_core
    "${FLUIDGRAIN_CSOUND_LIBRARY}" ${CMAKE_DL_LIBS})
  target_compile_features(fluidgrain_device_host PRIVATE c_std_11)
  if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(fluidgrain_device_host PRIVATE -Wall -Wextra -Wpedantic)
  endif()
  add_dependencies(fluidgrain_device_host fluidgrain)
  else()
    message(STATUS "Native device host unavailable: set FLUIDGRAIN_CSOUND_LIBRARY")
  endif()
endif()

if(FLUIDGRAIN_DESKTOP)
  include(cmake/desktop.cmake)
endif()

if(FLUIDGRAIN_BUILD_TESTS)
  # Optional C oracle for the separately run browser WGSL tests. No GPU
  # dependency is introduced into an ordinary native build or CTest run.
  add_executable(fluidgrain_gpu_reference EXCLUDE_FROM_ALL tests/gpu_reference.c)
  target_link_libraries(fluidgrain_gpu_reference PRIVATE fluidgrain_core)
  add_executable(fluidgrain_browser_bridge_test tests/test_browser_bridge.c)
  target_link_libraries(fluidgrain_browser_bridge_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.browser_bridge COMMAND fluidgrain_browser_bridge_test)
  enable_testing()
  if(TARGET fluidgrain_live)
    add_executable(fluidgrain_live_test tests/test_live.c)
    target_link_libraries(fluidgrain_live_test PRIVATE fluidgrain_live)
    add_test(NAME fluidgrain.live COMMAND fluidgrain_live_test)
  endif()
  if(FLUIDGRAIN_METAL)
    # Inject failure at the integration boundary without changing production
    # code or resetting a device shared with other apps. Works with Apple's ld.
    add_library(fluidgrain_metal_test_renderer OBJECT src/fluidgrain_native_gpu.c)
    target_link_libraries(fluidgrain_metal_test_renderer PRIVATE fluidgrain_core)
    target_compile_definitions(fluidgrain_metal_test_renderer PRIVATE fg_device_gpu_render=fg_test_device_render)
    add_executable(fluidgrain_metal_test tests/test_metal.c $<TARGET_OBJECTS:fluidgrain_metal_test_renderer>)
    target_link_libraries(fluidgrain_metal_test PRIVATE fluidgrain_core fluidgrain_metal)
    add_test(NAME fluidgrain.metal COMMAND fluidgrain_metal_test)
    set_tests_properties(fluidgrain.metal PROPERTIES TIMEOUT 90)
  endif()
  if(FLUIDGRAIN_CUDA AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
    add_executable(fluidgrain_gpu_worker_test tests/test_gpu_worker.c)
    target_link_libraries(fluidgrain_gpu_worker_test PRIVATE fluidgrain_native_gpu ${CMAKE_DL_LIBS})
    target_link_options(fluidgrain_gpu_worker_test PRIVATE -Wl,--wrap=fg_cuda_render)
    add_test(NAME fluidgrain.gpu_worker COMMAND fluidgrain_gpu_worker_test "$<TARGET_FILE:fluidgrain>")
    set_tests_properties(fluidgrain.gpu_worker PROPERTIES TIMEOUT 90)
    add_executable(fluidgrain_native_gpu_test tests/test_native_gpu.c)
    target_link_libraries(fluidgrain_native_gpu_test PRIVATE fluidgrain_native_gpu)
    target_link_options(fluidgrain_native_gpu_test PRIVATE -Wl,--wrap=fg_cuda_render)
    add_test(NAME fluidgrain.native_gpu COMMAND fluidgrain_native_gpu_test)
    set_tests_properties(fluidgrain.native_gpu PROPERTIES TIMEOUT 120)
  endif()
  # Separate, non-installed comparison plugin. The production target never
  # sees the shuffle definition or its extra storage/processing.
  add_library(fluidgrain_shuffle_core STATIC src/fluidgrain_core.c)
  target_compile_definitions(fluidgrain_shuffle_core PRIVATE FG_TEST_SPATIAL_SHUFFLE)
  target_link_libraries(fluidgrain_shuffle_core PUBLIC fluidgrain_core)
  set_target_properties(fluidgrain_shuffle_core PROPERTIES
    C_STANDARD 11 C_STANDARD_REQUIRED YES C_EXTENSIONS NO
    POSITION_INDEPENDENT_CODE YES C_VISIBILITY_PRESET hidden)
  if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(fluidgrain_shuffle_core PRIVATE -Wall -Wextra -Wpedantic -Wconversion)
  endif()
  add_library(fluidgrain_shuffle MODULE "${_fluidgrain_source}")
  target_compile_definitions(fluidgrain_shuffle PRIVATE BUILD_PLUGINS)
  target_include_directories(fluidgrain_shuffle SYSTEM PRIVATE
    "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
  target_link_libraries(fluidgrain_shuffle PRIVATE fluidgrain_shuffle_core)
  set_target_properties(fluidgrain_shuffle PROPERTIES
    C_STANDARD 11 C_STANDARD_REQUIRED YES C_EXTENSIONS NO C_VISIBILITY_PRESET hidden)
  if(TARGET sync_build_include_headers)
    add_dependencies(fluidgrain_shuffle sync_build_include_headers)
  endif()
  add_executable(fluidgrain_shuffle_test tests/test_shuffle.c)
  target_link_libraries(fluidgrain_shuffle_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.shuffle COMMAND fluidgrain_shuffle_test)
  add_executable(fluidgrain_view_test tests/test_view.c)
  target_link_libraries(fluidgrain_view_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.view COMMAND fluidgrain_view_test)
  if(UNIX)
    add_executable(fluidgrain_pack_plan EXCLUDE_FROM_ALL tools/pack_plan.c)
    target_link_libraries(fluidgrain_pack_plan PRIVATE fluidgrain_core)
    add_executable(fluidgrain_scheduler_plan EXCLUDE_FROM_ALL tools/scheduler_plan.c)
    target_link_libraries(fluidgrain_scheduler_plan PRIVATE fluidgrain_core)
  endif()
  add_executable(fluidgrain_grain_plan_test tests/test_grain_plan.c)
  target_link_libraries(fluidgrain_grain_plan_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.grain_plan COMMAND fluidgrain_grain_plan_test)
  add_executable(fluidgrain_core_test tests/test_core.c)
  target_link_libraries(fluidgrain_core_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.core COMMAND fluidgrain_core_test)
  add_executable(fluidgrain_extrema_test tests/test_extrema.c)
  target_link_libraries(fluidgrain_extrema_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.extrema COMMAND fluidgrain_extrema_test)
  add_executable(fluidgrain_fluid_test tests/test_fluid.c)
  target_link_libraries(fluidgrain_fluid_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.fluid COMMAND fluidgrain_fluid_test)
  add_executable(fluidgrain_resampler_test tests/test_resampler.c)
  target_link_libraries(fluidgrain_resampler_test PRIVATE fluidgrain_core)
  add_test(NAME fluidgrain.resampler COMMAND fluidgrain_resampler_test)
  if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    # Offline profiling only: production has no clocks, counters or wrappers.
    add_library(fluidgrain_profile MODULE "${_fluidgrain_source}" tests/profile_hooks.c)
    target_compile_definitions(fluidgrain_profile PRIVATE BUILD_PLUGINS)
    target_include_directories(fluidgrain_profile SYSTEM PRIVATE
      "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
    target_link_libraries(fluidgrain_profile PRIVATE fluidgrain_core)
    target_link_options(fluidgrain_profile PRIVATE
      -Wl,--wrap=fg_field_step -Wl,--wrap=fg_memory_size
      -Wl,--wrap=malloc -Wl,--wrap=calloc -Wl,--wrap=realloc -Wl,--wrap=free)
    set_target_properties(fluidgrain_profile PROPERTIES
      C_STANDARD 11 C_STANDARD_REQUIRED YES C_EXTENSIONS NO C_VISIBILITY_PRESET hidden)
    if(TARGET sync_build_include_headers)
      add_dependencies(fluidgrain_profile sync_build_include_headers)
    endif()
    add_executable(fluidgrain_allocation_test tests/test_allocations.c)
    target_link_libraries(fluidgrain_allocation_test PRIVATE fluidgrain_core)
    target_link_options(fluidgrain_allocation_test PRIVATE
      -Wl,--wrap=fg_field_step -Wl,--wrap=fg_read_bandlimited -Wl,--wrap=malloc -Wl,--wrap=calloc -Wl,--wrap=realloc -Wl,--wrap=free)
    add_test(NAME fluidgrain.allocations COMMAND fluidgrain_allocation_test)
  endif()
  find_package(Python3 3.8 COMPONENTS Interpreter REQUIRED)
  if(UNIX)
    find_package(Threads REQUIRED)
    add_executable(fluidgrain_provider_test tests/test_provider.c)
    target_link_libraries(fluidgrain_provider_test PRIVATE fluidgrain_core Threads::Threads)
    add_test(NAME fluidgrain.provider COMMAND fluidgrain_provider_test)
    set_tests_properties(fluidgrain.provider PROPERTIES TIMEOUT 120)
    add_executable(fluidgrain_transport_test tests/test_transport.c)
    target_link_libraries(fluidgrain_transport_test PRIVATE fluidgrain_core Threads::Threads)
    add_test(NAME fluidgrain.transport COMMAND fluidgrain_transport_test)
    add_test(NAME fluidgrain.packet_wire COMMAND "${Python3_EXECUTABLE}"
      "${CMAKE_CURRENT_LIST_DIR}/tests/test_packet_wire.py" "$<TARGET_FILE:fluidgrain_transport_test>")
    set_tests_properties(fluidgrain.transport PROPERTIES TIMEOUT 120)
  endif()
  add_test(NAME fluidgrain.package COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_LIST_DIR}/tests/test_package.py")
  add_test(NAME fluidgrain.schema COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_LIST_DIR}/tools/generate_schema.py" --check)
  set(CSOUND_EXECUTABLE "" CACHE FILEPATH "Csound 7 executable")
  if(NOT CSOUND_EXECUTABLE)
    find_program(_fluidgrain_csound NAMES csound csound64 HINTS "${HLOLLI_CSOUND_BUILD_DIR}")
    if(_fluidgrain_csound)
      set(CSOUND_EXECUTABLE "${_fluidgrain_csound}")
    endif()
  endif()
  if(CSOUND_EXECUTABLE AND NOT FLUIDGRAIN_SANITIZE)
    add_test(NAME fluidgrain.csound COMMAND "${Python3_EXECUTABLE}" "${CMAKE_CURRENT_LIST_DIR}/tests/test_csound.py" --csound "${CSOUND_EXECUTABLE}" --module "$<TARGET_FILE:fluidgrain>")
    set_tests_properties(fluidgrain.csound PROPERTIES TIMEOUT 120)
  endif()
  find_library(FLUIDGRAIN_CSOUND_LIBRARY NAMES csound64 csound CsoundLib64
    HINTS "${HLOLLI_CSOUND_BUILD_DIR}" "${CMAKE_CURRENT_LIST_DIR}/../csound/build"
    DOC "Matching Csound 7 API library for the prepared-host lifecycle test")
  if(FLUIDGRAIN_CSOUND_LIBRARY)
    if(FLUIDGRAIN_NATIVE_WORKER)
      add_executable(fluidgrain_buffered_opcode_test tests/test_buffered_opcode.c)
      target_include_directories(fluidgrain_buffered_opcode_test SYSTEM PRIVATE
        "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
      target_link_libraries(fluidgrain_buffered_opcode_test PRIVATE fluidgrain_core
        "${FLUIDGRAIN_CSOUND_LIBRARY}" ${CMAKE_DL_LIBS})
      target_compile_definitions(fluidgrain_buffered_opcode_test PRIVATE FG_TEST_DEVICE_GPU=$<OR:$<BOOL:${FLUIDGRAIN_CUDA}>,$<BOOL:${FLUIDGRAIN_METAL}>>)
      add_dependencies(fluidgrain_buffered_opcode_test fluidgrain)
      add_test(NAME fluidgrain.buffered_opcode COMMAND fluidgrain_buffered_opcode_test "$<TARGET_FILE:fluidgrain>")
      set_tests_properties(fluidgrain.buffered_opcode PROPERTIES
        WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}" TIMEOUT 45)
    endif()
    if(FLUIDGRAIN_CUDA)
      add_executable(fluidgrain_gpu_opcode_test tests/test_gpu_opcode.c)
      target_include_directories(fluidgrain_gpu_opcode_test SYSTEM PRIVATE
        "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
      target_link_libraries(fluidgrain_gpu_opcode_test PRIVATE "${FLUIDGRAIN_CSOUND_LIBRARY}" m)
      add_dependencies(fluidgrain_gpu_opcode_test fluidgrain)
      add_test(NAME fluidgrain.gpu_opcode COMMAND fluidgrain_gpu_opcode_test "$<TARGET_FILE:fluidgrain>")
      set_tests_properties(fluidgrain.gpu_opcode PROPERTIES
        WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}" TIMEOUT 120)
    endif()
    add_executable(fluidgrain_plan_host_test tests/test_plan_host.c)
    target_include_directories(fluidgrain_plan_host_test SYSTEM PRIVATE
      "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
    target_link_libraries(fluidgrain_plan_host_test PRIVATE "${FLUIDGRAIN_CSOUND_LIBRARY}" m)
    target_compile_features(fluidgrain_plan_host_test PRIVATE c_std_11)
    if(FLUIDGRAIN_SANITIZE AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
      target_compile_options(fluidgrain_plan_host_test PRIVATE -fsanitize=address,undefined,float-cast-overflow -fno-omit-frame-pointer)
      target_link_options(fluidgrain_plan_host_test PRIVATE -fsanitize=address,undefined,float-cast-overflow)
    endif()
    add_dependencies(fluidgrain_plan_host_test fluidgrain)
    add_test(NAME fluidgrain.plan_host COMMAND fluidgrain_plan_host_test "$<TARGET_FILE:fluidgrain>")
    set_tests_properties(fluidgrain.plan_host PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}" TIMEOUT 120)
    add_executable(fluidgrain_host_test tests/test_host.c)
    target_include_directories(fluidgrain_host_test SYSTEM PRIVATE
      "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
    target_include_directories(fluidgrain_host_test PRIVATE "${CMAKE_CURRENT_LIST_DIR}/include")
    target_link_libraries(fluidgrain_host_test PRIVATE "${FLUIDGRAIN_CSOUND_LIBRARY}")
    if(UNIX)
      target_link_libraries(fluidgrain_host_test PRIVATE m)
    endif()
    target_compile_features(fluidgrain_host_test PRIVATE c_std_11)
    if(FLUIDGRAIN_SANITIZE AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
      # Instrument the API host too: do not load an ASan plugin into the
      # uninstrumented command-line Csound process.
      target_compile_options(fluidgrain_host_test PRIVATE -fsanitize=address,undefined,float-cast-overflow -fno-omit-frame-pointer)
      target_link_options(fluidgrain_host_test PRIVATE -fsanitize=address,undefined,float-cast-overflow)
    endif()
    if(UNIX)
      add_executable(fluidgrain_worker_host examples/cpu-worker.c)
      target_include_directories(fluidgrain_worker_host SYSTEM PRIVATE
        "$<TARGET_PROPERTY:fluidgrain,INCLUDE_DIRECTORIES>")
      target_include_directories(fluidgrain_worker_host PRIVATE
        "${CMAKE_CURRENT_LIST_DIR}/include" "${CMAKE_CURRENT_LIST_DIR}/src")
      target_link_libraries(fluidgrain_worker_host PRIVATE
        "${FLUIDGRAIN_CSOUND_LIBRARY}" Threads::Threads ${CMAKE_DL_LIBS} m)
      target_compile_features(fluidgrain_worker_host PRIVATE c_std_11)
      if(FLUIDGRAIN_SANITIZE AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
        target_compile_options(fluidgrain_worker_host PRIVATE -fsanitize=address,undefined,float-cast-overflow -fno-omit-frame-pointer)
        target_link_options(fluidgrain_worker_host PRIVATE -fsanitize=address,undefined,float-cast-overflow)
      endif()
      add_dependencies(fluidgrain_worker_host fluidgrain)
      add_test(NAME fluidgrain.worker_host COMMAND fluidgrain_worker_host "$<TARGET_FILE:fluidgrain>")
      set_tests_properties(fluidgrain.worker_host PROPERTIES
        WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}" TIMEOUT 120)
    endif()
    add_dependencies(fluidgrain_host_test fluidgrain)
    add_test(NAME fluidgrain.host COMMAND fluidgrain_host_test "$<TARGET_FILE:fluidgrain>")
    set_tests_properties(fluidgrain.host PROPERTIES
      WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}" TIMEOUT 120)
    if(TARGET fluidgrain_profile AND NOT FLUIDGRAIN_SANITIZE)
      add_test(NAME fluidgrain.profile COMMAND "${Python3_EXECUTABLE}"
        "${CMAKE_CURRENT_LIST_DIR}/tests/test_profile.py"
        --module "$<TARGET_FILE:fluidgrain>"
        --profile-module "$<TARGET_FILE:fluidgrain_profile>"
        --library "${FLUIDGRAIN_CSOUND_LIBRARY}")
      set_tests_properties(fluidgrain.profile PROPERTIES TIMEOUT 240 SKIP_RETURN_CODE 77)
    endif()
  else()
    message(STATUS "Prepared-host test unavailable: set FLUIDGRAIN_CSOUND_LIBRARY to the matching Csound 7 library")
  endif()
endif()
